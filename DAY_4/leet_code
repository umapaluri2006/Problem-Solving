#3146 perumutation difference between two strings
class Solution:
    def findPermutationDifference(self, s: str, t: str) -> int:
        diff = 0
        for i in range(len(s)):
            c = s[i]
            j = t.find(s[i])
            diff+=abs(j-i)
            
        return diff
