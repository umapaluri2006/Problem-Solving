#1065 counting problem
# cook your dish here
t = int(input())
for _ in range(t):
    n = int(input())
    a = list(map(int,input().split()))
    tot = sum(a)
    odd = any(x%2!=0 for x in a)
    if tot%2==0 and odd:
        print("YES")
    else:
        print("NO")
